import React from 'react'
import AuthCard from '../components/AuthCard'

/**
 * @returns AuthCard function component which takes the type prop (which is signup at this point) and returns signup page.
 */

export default function Signup() {
  return (
    <AuthCard type="signup"/>
  )
}
